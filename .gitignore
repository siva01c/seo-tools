# This file tells Git which files shouldn't be added to source control

.idea
dist
node_modules
storage
.history
.vscode
.claude

# Added by Apify CLI
.venv
.env
# Local deployment overlay (e.g. attaching the MCP server to a shared network)
docker-compose.override.yml
